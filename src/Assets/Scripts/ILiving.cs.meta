using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface ILiving {
    // Reports what type of living thing it is
    string getType();

    // Eating either plants, or animals
    void Consume();

    // Reproduce, regardless of how it is down
    void Reproduce();

    //decrement energy depending on traits
    void EnergyTick();
}
